# Shapeshifting-LAN---FYP



### For setup
```
sudo apt update
sudo apt install mininet python3-ryu
pip install scapy --break-system-packages
```


## Quick Start (VM Image)
Download the pre-built VM: [link]
Import into VirtualBox/VMware and run.

## Manual Setup (Vagrant)
git clone https://github.com/EbiScott/Shapeshifting-LAN---FYP
cd Shapeshifting-LAN---FYP
vagrant up
vagrant ssh
